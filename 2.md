Bu görev sadece "Terminalden" Yapılacak

**Müşteri:** Caner K. (Sistem Operatörü) **Konu:** Dosyayı okumak isteyince "Invalid Option" hatası veriyor!

_"Selamlar, `/tmp/ctf3/` dizininde benim oluşturduğum bir not dosyası var. Terminalde `ls` yaptığımda dosyayı açıkça görüyorum. Fakat `cat` ile okumaya kalktığımda veya `rm` ile silmek istediğimde sistem bana **'invalid option' (geçersiz seçenek)** şeklinde garip bir hata fırlatıyor. Dosya resmen komutları bozuyor! Lütfen şu dosyanın içindeki veriyi kurtar ve dosyayı sil."_

### 🛠️ Senaryoyu Başlatma Komutu

Bu senaryoyu makinenize kurmak için aşağıdaki komut bloğunu kopyalayıp terminalinize yapıştırın:

Bash

```
echo "bWtkaXIgLXAgL3RtcC9jdGYzOyBlY2hvICdDVEZ7ZDRzaF9jNG5fYjNfdHIxY2t5fScgPiAvdG1wL2N0ZjMvLWdpemxpbm90LnR4dDsgZWNobyAnWytdIFNlbmFyeW8gaGF6aXIhIEhlZGVmIGRpemluOiAvdG1wL2N0ZjMvJw==" | base64 -d > ctf3.sh && chmod +x ctf3.sh && ./ctf3.sh
```

**Görev:** `/tmp/ctf3/` dizinine git. Müşterinin bu dosyayla neden işlem yapamadığını bul, içindeki "CTF{...}" bayrağını oku.
